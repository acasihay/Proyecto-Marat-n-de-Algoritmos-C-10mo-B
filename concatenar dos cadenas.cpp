#include <bits/stdc++.h>
using namespace std;
int main(){
    string a,b; if(!(cin>>ws); !getline(cin,a)) return 0; getline(cin,b);
    // si entrada complicada usamos >> para dos tokens simples:
    // cin>>a>>b;
    cout<<a<<b<<'\n';
    return 0;
}
